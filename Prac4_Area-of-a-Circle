#include <stdio.h>
#include <math.h>

int main ()

{
	int radius;
	float area;
	
	printf("Give me the radius of the circle");	scanf("%d", &radius);
	
	area = M_PI * pow(radius, 2);
	
	printf("The Area of that cicle is %f", area);
	
}
