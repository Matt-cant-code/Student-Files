#include <stdio.h>

int main ()

{
	
	int minutes;
	int hours;
	
	printf("How many minutes?: ");
	scanf("%d", &minutes);
	
	hours = minutes / 60;
	minutes = minutes % 60;
	
	printf("That is %d hour/s and %d minute/s", hours, minutes);
	
	return 0;
	
}
